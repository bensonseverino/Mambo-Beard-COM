# react-helmet-async 2.0.5 (pinned — the stable, dedupe-correct version;
# 3.0.0 duplicates head tags under React 19) declares peer ranges that
# predate React 19, so installs must allow legacy peer resolution.
legacy-peer-deps=true
